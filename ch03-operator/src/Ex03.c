/*

//산술연산자
//- 수치 형식의 데이터를 다루는 연산자
//- 덧셈(+), 뺄셈(-), 곱셉(*), 나눗셈(/), 나머지(%)
//- 두개의 피연산자가 필요한 이항연산자
//- 정수형식과 소수점 형식에서도 사용가능
//- *, /은 +와 -보다 먼저 실행되는 연산자

#include <stdio.h>

int main(void){

	int a, b;
	a = 2;
	b = 7;

	printf("덧셈 연산결과: %d\n", a+b);
	printf("뺄셈 연산결과: %d\n", a-b);
	printf("곱셈 연산결과: %d\n", a*b);
	printf("나누기 연산결과: %d\n", a/b);
	printf("나머지 연산결과: %d\n", a%b);

	return 0;
}
*/
