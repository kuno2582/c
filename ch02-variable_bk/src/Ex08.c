/*

//상수를 기호화하여 변수처럼 이름이 있는 상수 선언(대문자)
//	- const키워드 이용해서 상수 정의
//->- #define문을 이용해서 상수 정의

#include <stdio.h>

#define PI 3.14
#define NUM 1000
#define FILE_SIZE 1024
#define BUFFER_SIZE 256

int main(){

	printf("%lf \n", PI);
	printf("%d \n", NUM);
	printf("%d \n", FILE_SIZE);
	printf("%d \n", BUFFER_SIZE);
}
*/
