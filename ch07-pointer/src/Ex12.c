/*


#include <stdio.h>

int main(void){

	int array[3]={10, 20, 30};
	int* p=NULL;

	p=array;
	//배열 요소의 값(배열)
	printf("%d %d %d\n", array[0], array[1], array[2]);
	printf("%d %d %d\n", *(array+0), *(array+1), *(array+2));

	//배열 요소의 값(포인터)
	printf("%d %d %d\n", p[0] , p[1], p[2]);
	printf("%d %d %d\n", *(p+0), *(p+1), *(p+2));

	//배열의 크기
	printf("배열의 크기: %d\n", sizeof(array));

	//포인터 변수의 크기
	printf("포인터의 크기: %d\n", sizeof(p));

	return 0;
}
*/
