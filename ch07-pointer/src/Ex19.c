/*


//문자열 특징
//-""(큰따옴표)내에 포함된 하나이상의 문자
//-문자열의 끝에는 문자열의 끝을 알리는 널문자(Null),
// 종료문자(\0)가 삽입됩니다.
//-문자열의 시작주소를 알면 저장된 문자들에 접근 할 수 있습니다.
//-문자열은 문자열 상수와 문자열 변수(문자 배열, 문자열 배열)로 구분
//-문자열을 출력할 때는 서식문자 %s를 사용

//"codedragon"
//"c"

#include <stdio.h>

int main(void){

	printf("ABCDE");

	printf("%d", sizeof("ABCDE"));

	return 0;
}
*/
